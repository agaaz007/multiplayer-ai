export const common=`Use the installed product's native persistence and retrieval throughout work. The successor starts in a fresh checkout and fresh conversation; previous worktree files are unavailable. Save original deliverables and supporting documents, not just a summary. Locate the predecessor's actual artifacts, verify their provenance and scope, and recover code through the product when useful. Preserve uncertain operation outcomes and check evidence before retrying a mutation. A saved or stable agent recommendation does not establish stakeholder approval. Preserve who decided, whether they had authority, what remains proposed, and any later reversal. Do not equate PROPOSED state with confirmed decisions.\n`;
export const guides={
 'control-git':`This lane shares work only through Git. No memory product, MCP memory server or capture hook is configured, and nothing else is transported between teammates. A bare shared repository ({root}/shared-remote.git) is configured as the \`origin\` remote of this workspace; it is the only durable channel.
Start every stage with \`git fetch origin\`, then inspect \`git branch -r\`, \`git log --all --oneline --decorate\` and \`git show <ref>:<path>\` to recover predecessors' work. Restore recovered files into this worktree (for example \`git checkout origin/<branch> -- <path>\`) while keeping this stage's newly released inputs. The controller copies nothing from predecessors; a branch that was never pushed is not recoverable.
Commit as you go and push to origin (for example \`git push origin HEAD:refs/heads/stage-<X>\`) BEFORE your final task action, and again after any later change. Stage B is killed at the supplier boundary immediately after its final submit; work that is unpushed at that moment is lost and stays lost. Record decisions with their authority and status, superseded work, open questions and the pending operation key and status in committed files so successors can read them from history. Treat recovered claims as a teammate's claims, not verified facts.
`,
 'handoff-note':`This lane shares work only through a markdown handoff note. No memory product, MCP memory server or capture hook is configured. Previous teammates' notes, when they exist, are readable at {root}/handoff-notes/ (one file per stage, for example A.md, B.md). Read them first and treat their claims as a teammate's claims, not verified facts.
Before your final task action, write HANDOFF.md at the root of this workspace. After the session ends the controller copies that single file to {root}/handoff-notes/<stage>.md; nothing else is transported, and a missing note is recorded as absent. Predecessor code, scripts, sources and reports are NOT carried forward unless their content is inside the note, so include any code, data and exact values a successor needs, plus decisions with their authority and status, superseded work, the pending operation key and status, open questions and next steps. Stage B is killed at the supplier boundary immediately after its final submit; a note not written by then is lost.
`,
 ledger:common+`Ledger has its native PostgreSQL event/artifact store, thread snapshots, work records, scoped resume packs, explicit object tools and automatic classifier enabled. The helper captures real transcripts and pushes original WIP snapshots every 30 seconds and at final capture. The native classifier produces proposals; inspect their original evidence before accepting them. During readiness, create a work record, update it after the released correction with evidence and the exact pending key, confirm only the supported work state, and have the fresh successor retrieve that same confirmed record before looking up the key. Confirmation by an agent is not human approval. In scored work use records naturally and preserve any failure; the audit reports actual use separately from enabled configuration.
At startup use ledger_records and ledger_threads, then use ledger_record_get and ledger_resume(record_id/thread_id) for the relevant predecessor. If no matching work exists, create a work record with ledger_record_start for this actual goal and link the current work; subsequent sessions should resume that record. Explicitly inspect the returned record/thread evidence and snapshot bootstrap rather than relying on a summary alone. For code, use the resume pack's native Git bootstrap commands to fetch the verified snapshot from the owned remote, inspect it, then restore relevant files into this current worktree; retain current task inputs when recovering old work. Check saved-through/remote verification and actual hashes. Never assume a pending external operation failed because its result is absent.
Use ledger_events and ledger_evidence_search to retrieve exact prior event sequences, and ledger_artifact_get for complete available tool inputs/outputs behind previews. Resume can refer to artifacts and snapshots rather than placing every byte in its prose. Inspect capture gaps and oversized flags. Known short-file integration gap: a raw worktree SHA is not an acceptance evidence artifact ID. A captured short preview may have no retained artifact reference. Inspect actual artifact_refs and use only a successfully retrieved retained original where the API requires one; otherwise report the failed acceptance. Do not pad files, fabricate evidence, or omit required acceptance fields to bypass validation. Use records across sessions, explicit span links, evidence-backed updates, thread notes, and stable definitions/findings/decisions only when justified. Preserve substantive documents/code in the worktree so snapshots contain them. The harness also records native final export evidence; it never reconstructs a semantic handoff for you.
`,
 gbrain:common+`GBrain 0.50.0.0 uses a private PostgreSQL brain, native local attachment StorageBackend, full MCP surface and current native AI gateway. Start with context_pack, delta, recall, entity, and relevant get_page/query results. Persist useful facts with remember and extract_facts; inspect original provenance, validity and contradictions through ontology_get/ontology_conflicts/find_trajectory. Use capture/put_page for full substantive documents, tags/links/traverse_graph/timeline for relationships, get_versions/revert_version for corrections. Use synthesize for cited multi-page reasoning when needed, and verify its source evidence. OpenAI gpt-4.1-mini is explicitly configured for native expansion, extraction and synthesis; the controller applies a declared 4096-token per-response ceiling and the shared dollar budget. Native balanced search has expansion explicitly enabled and uses the documented local Qwen3-Reranker-4B recipe with the pinned community GGUF conversion; inspect rerank_score and native degraded indicators instead of assuming reranking succeeded. Current upstream semantic query caching is temporarily disabled by the product.
Preserve original files incrementally with gbrain_upload_attachment (native file_upload) or gbrain_capture_workspace(stage). Current native file_upload/file_list/file_url are local-only operations, so file transport exposes their supported backend without pretending they are upstream MCP tools. Native file_url now resolves a real backend URL and verifies that the file exists. Code import uses GBrain's own auto strategy, AST symbols and relations; query gbrain_files.gbrain_code_def/gbrain_code_refs/gbrain_code_callers/gbrain_code_callees/gbrain_code_blast/gbrain_code_flow before changing recovered implementation. These six tools call the documented trusted-local native CLI in the owned brain; upstream gbrain.code_* MCP handlers are suspended for agent callers, so route code reads through gbrain_files rather than retrying the denied MCP tools.
Automatic final capture, including interruption, ingests the complete current workspace and actual session transcript as full native pages and exact original attachments. Text is verbatim and split only above native input limits; all original bytes remain in attachments. The controller-produced manifest contains path/hash/storage provenance only. The hook also invokes current native Codex transcript ingestion with embedding and fact extraction; that native parser redacts secrets and imports human/assistant turns, while original transcript attachments preserve tool events and pending-operation evidence. This is a configured ingestion hook using the supported native transcript importer, not a controller semantic handoff.
Find producer artifacts with get_page on stage-a-artifact-manifest, stage-b-artifact-manifest, etc., or search/query. get_page returns full compiled_truth; search snippets are not complete documents. Manifests identify original path, source_kind, page_slugs, native storage_path, hash and bytes. Use gbrain_read_attachment (paginated exact base64 and full-file hash) or gbrain_restore_attachment (current-workspace destination and explicit overwrite). These transport tools use GBrain's native StorageBackend.download. Use original transcript evidence for uncertain operations and retain current task delta while recovering earlier code.
`,
 graphify:common+`Graphify uses a private complete original-file corpus, official local AST parsing, and the configured official semantic extraction backend. Preserve work incrementally with graphify_capture_workspace(stage), graphify_write_source and graphify_save_result. Use graphify_extract for full AST plus semantic indexing; code_only deliberately omits document semantics and is not full readiness. A final automatic configured ingestion hook also preserves the complete workspace and original current-session transcript and runs official extraction after the agent ends or is interrupted. No semantic controller summary is added.
The original corpus retains nested files under stages/<stage>/<original-path>, plus original session files under sessions/<stage>/. Use graphify_query (BFS or DFS, budget at least 100, optional contexts), graphify_path, graphify_explain, graphify_affected with relation/depth filters, graphify_god_nodes, native saved feedback and graphify_reflect. Graph answers are pointers to evidence, not substitutes for original files.
The separate upstream graphify_native server also exposes query_graph, get_node, get_neighbors, get_community, graph_stats, shortest_path and PR impact tools against this same graph. Use node/community inspection to follow native query evidence. Use graphify_list_sources for the full path/hash inventory. Read originals completely with graphify_read_source and next_offset until null; content is exact base64 so decode UTF8 for text. Recover exact producer code/documents with graphify_restore_source(id,destination,sha256,overwrite). Destination is always this current worktree. Existing differing bytes require explicit overwrite; preserve newly supplied task delta/requirements while recovering old implementation files. Native graph/cache files live outside the original corpus. Graphify itself does not implement decision authority, classifier threads, or unknown-outcome execution transactions; use original evidence and your judgment.
`,
 supermemory:common+`Use the pinned official Codex plugin's recall and capture hooks and project-scoped native MCP. The supplementary supermemory_files tools expose the current native API: hybrid search with query rewriting and reranking by default (aggregation is available with reranking disabled), project profile, full document reads, and original-file upload/download. Use supermemory_search and supermemory_profile to locate current and superseded memory; inspect full documents and source evidence before carrying a correction forward. A profile is not proof of stakeholder approval.
Save substantive work through the native memory API. Use supermemory_upload_file or supermemory_capture_workspace to retain exact original UTF8 code, reports, sources and state. These use native multipart file upload, fileType=text, memory extraction and instant processing; no controller semantic summary is written. The file-size ceiling matches the other native artifact transports, while the same shared dollar budget remains enforced. Arbitrary binary/rich-content support and pricing are not assumed; unsupported content and failed uploads remain visible. No base64 substitute is called native binary storage.
Find stage-a-artifact-manifest, stage-b-artifact-manifest and later documents by custom ID or search. Index metadata contains original paths, SHA256 and native document IDs only. Restore originals via supermemory_restore_file with their expected hash before modifying inherited work. Native presigned download hosts must be pinned from readiness, and URLs never carry provider authorization headers. Search excerpts and extracted memories are not original-file bytes. Automatic final capture imports original workspace/transcript files through the same native backend; the official plugin also captures its native conversation memory. Inspect processing status and actual download hashes, and retain corrections, proposals, scope and the pending supplier key using native memory updates.
`,
};

/** Guide text for an arm; control guides carry the absolute transport path of their sequence root. */
export function guideFor(arm,root){const text=guides[arm];if(!text)throw new Error('no guide for arm '+arm);return root?text.split('{root}').join(root):text;}
