Fresh Claude completed Rachit's interrupted Analysis design-readiness helper through Ledger. A second fresh Claude completed it through GBrain with the same saved-code access and source history. Both preserved the original behavior and passed independent validation. This establishes task continuation in this case; it does not establish that Ledger's structured work records improve accuracy.

| Check | Claude + Ledger | Claude + GBrain |
| --- | --- | --- |
| Recovered original task files before editing | Yes, full WIP worktree | Yes, two files extracted from WIP |
| Unchanged source tests | 4/4 pass | 4/4 pass |
| Frozen independent checks | 68/68 pass | 68/68 pass |
| Focused TypeScript check by evaluator | Pass | Pass |
| Initial continuation duration | 152 seconds | 162 seconds |
| Follow-up: four requested context facts with sources | 4/4 | 4/4 |
| Follow-up duration / memory tool calls | 35 seconds / 3 | 228 seconds / 53 |

The source snapshot passed only 2/4 original tests and 20/68 independent checks. The successors each added the missing `draft_archived` and `unsaved_edits` guards while preserving the four required fields, input immutability, and `measurement: 'unreviewed'`. GBrain also added the test file to the existing `test:analysis` command. The original test file is byte-for-byte unchanged in both results. These are checks within one task, not 68 independent tasks or a reliability estimate.

The [Ledger completion patch](../../.context/real-handoff-20260910/ledger-completed-task-from-base.patch) includes Rachit's recovered files and Claude's completion, and applies to base `09980df36b97461a94533f2d3527ba334dc957e4`. The [GBrain completion patch](../../.context/real-handoff-20260910/gbrain-completed-task-from-base.patch) includes its additional test-command wiring. Both patches passed `git apply --cached --check` against that base. Separate continuation-only patches apply to the WIP snapshot. Ledger's successor made local commit `b5d2a31`; GBrain left local changes uncommitted. Nothing was pushed, merged, or deployed.

Rachit deliberately stopped this small task in repository `agaaz007/behaviour-md-tranzmit`, on his `MacBook-Pro.local`, using Codex session `01a08156-09f7-77f1-8bf0-c1d07b0c518a`, thread `3aa42a20-0a67-4cd4-99dd-3f7d617bf800`. The exact snapshot fetched from GitHub was `refs/wip/rachit/01a08156-09f7-77f1-8bf0-c1d07b0c518a` at `1ce6fa161b7f81f88e7f885d3f23d06ac7aaf1fb`. It was remotely verified at 2026-09-09 16:05:28 UTC, or 21:35:28 IST. The source feature branch was never pushed; ordinary commits were not required to save its untracked files. This is a new task, distinct from the older adaptive-learning checkout whose final files remain unrecovered.

The frozen input contains 1,176 captured events, three checkpoints, and 202 artifacts from that source session, including unrelated product and capture-diagnostic history. Both successors used Claude Code 2.1.258, `claude-sonnet-5`, authenticated through the user's subscription. Each started at the same Git base with the same one-line request to continue Rachit's helper. Their operational instructions required memory retrieval, Git verification, local work, and evidence in the final response; they contained no task recap or expected implementation.

Ledger used its native tools against a private copy of the actual source rows and a frozen local build. GBrain 0.18.2 imported the same rows, full inline artifacts, and snapshot metadata into 1,379 Markdown pages, with no import failures and no embeddings. Its retrieval tools therefore operated on a keyword index. The shared history includes earlier Ledger resume-pack tool outputs. This compares continuation with equal information and saved-code access; it does not test GBrain's own capture mechanism or a separately curated GBrain knowledge base.

There were **no classified work records** in the source. None were manufactured for this evaluation. Ledger's successor used `ledger_resume(mode: "inspect")`; it did not exercise a production continuation claim, release, or new automatic snapshot. Both runs and their source stores were isolated from production writes and each other. This evaluates real source work recovered from another laptop, with the comparison itself run in isolated stores on Agaaz's laptop. Rachit's laptop being offline remains unconfirmed.

The evaluator froze the grader before launching either successor, verified the source failed it, and ran it independently afterward. The 64 combination checks cover required-field whitespace, archived/draft status, unsaved edits, blocker accumulation, repeatability, and deep-frozen inputs; four extra probes check that measurement status does not become a gate, including defensive values outside the TypeScript enum. The focused typecheck uses the unchanged real `AnalysisDraft` module and the local TypeScript compiler. It is not a complete dashboard build. An initial evaluator configuration used a removed `baseUrl` option; the corrected configuration passed, and both attempts are retained.

The successors could not run a complete project typecheck because dependencies were absent. GBrain additionally ran the broader analysis command and reproduced a missing `@beirut/shared` workspace-link failure on the unchanged base. Neither claimed that a full typecheck passed. Production UI parity remains unverified, as it was in the source. The helper is not wired into the UI, consistent with the narrowly scoped original task.

A separate follow-up asked each successor for the source constraint, rejected approach and reason, unresolved verification, and unrelated topic, with evidence. Both retrieved source event 646 and correctly identified the no-model/production-write/metric-change constraints, rejection of a second JSON planning form because the draft already contains those fields, unverified production UI parity, and the meeting preference to start with before/after screens. The evaluator checked both answers against actual retrieved tool results in the [context-probe audit](../../.context/real-handoff-20260910/context-probe-audit.json). Both scored 4/4 on those specified categories; Ledger needed three memory calls and GBrain 53. These single-run timings do not establish a general speed advantage, and prompted recall does not prove the facts caused initial implementation success.

The extra prose exposed temporal weaknesses that the four-category score does not capture. GBrain falsely said the missing-capture/snapshot issue was not resolved before the source session stopped, contradicting the verified checkpoint and its own successful Git recovery. Ledger cited an earlier pending-snapshot statement without explaining its later resolution. Neither result supports a claim that all recalled state was current. The source thread listing also has a known freshness-label defect: it checks the head checkpoint's null verification field instead of falling back to the earlier verified checkpoint. This was documented, not fixed as part of the test.

The [protocol](../../.context/real-handoff-20260910/protocol.json), [implementation audit](../../.context/real-handoff-20260910/audit.json), original and completed files, patches, native transcripts, tool traffic, independent grader, and reproduction scripts are retained under `.context/real-handoff-20260910/`. Global hooks, settings, automatic memory, and other MCP servers were disabled. The filesystem boundary was behavioral rather than an OS sandbox; actual tool traffic was reviewed for cross-condition reads. The original private grading checklist was not found in the captured material and was not supplied to either successor; grading against it remains pending.

The controller-created local PostgreSQL database was dropped after both successors and their follow-ups exited; a catalog query verified it absent. Source exports, the isolated successor's local finding, and both sessions' own spilled tool artifacts were retained. The result was recorded in shared Ledger as `fnd-20260909-real-rachit-to-claude-handoff-completed-in-both--v8ho`, committed and pushed. Feature code remains local.

This is one Codex-to-Claude source task with two comparison conditions. The [earlier controlled tests](handoff-evaluation-2026-09-09.md) separately cover concurrent agents and both harness directions. This run does not complete the reverse-direction long-history test, exercise native compaction, or establish a general memory-accuracy advantage.
