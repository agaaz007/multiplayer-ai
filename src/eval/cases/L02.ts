import type { EvalCaseRunner } from "./index.js";

/** L02 (phase 2): needs the classifier delayed behind capture, cursor readings, and coverage.json from the real resume output. */
export const L02: EvalCaseRunner = { id: "L02", skip: "phase 2: classifier lag control, cursor readings and coverage.json collector not implemented" };
