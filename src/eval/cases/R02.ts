import type { EvalCaseRunner } from "./index.js";

/** R02: two contributors across harnesses. The drivers swap people/harness per direction; nothing extra here. */
export const R02: EvalCaseRunner = { id: "R02" };
