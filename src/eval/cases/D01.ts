import type { EvalCaseRunner } from "./index.js";

/** D01: mid-session constraint and rejected approach. Common flow; the origin types the four events in order. */
export const D01: EvalCaseRunner = { id: "D01" };
