import type { EvalCaseRunner } from "./index.js";

/** D03: two hypotheses on different snapshots stay unresolved. Common flow; session-a/session-b authors come from the fixture. */
export const D03: EvalCaseRunner = { id: "D03" };
