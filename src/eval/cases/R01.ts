import type { EvalCaseRunner } from "./index.js";

/** R01: five interleaved topics in one session. Nothing extra to collect; `selected_topic` comes from the successor's output. */
export const R01: EvalCaseRunner = { id: "R01" };
