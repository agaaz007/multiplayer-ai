import type { EvalCaseRunner } from "./index.js";

/** E03 (phase 2): needs the successor to edit layout.json in the recovered worktree and the collector to copy final/ after it finishes. */
export const E03: EvalCaseRunner = { id: "E03", skip: "phase 2: successor edit-and-validate flow with final/ file bundle not implemented" };
