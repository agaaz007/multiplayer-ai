"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "npm ci"
run_mode = "concurrent"

[scripts.run.ledger]
available_in = ["local"]
command = 'LEDGER_UI_PORT="${CONDUCTOR_PORT:-4318}" npm run preview:ui'
default = true
icon = "play"
