"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "bash scripts/conductor.sh setup"
run_mode = "concurrent"

[scripts.run.ledger]
available_in = ["local"]
command = "bash scripts/conductor.sh preview"
default = true
icon = "play"
